# Exported from Render on 2026-04-08T13:30:05Z
# NOTE: Update the repo URL below to point to your fork before connecting this service in Render.
version: "1"
services:
  - type: web
    name: hubspot-recommendation-tool
    runtime: docker
    repo: https://github.com/SerenePrince/hubspot-recommendation-tool
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        sync: false
      - key: AUTH_ENABLED
        sync: false
      - key: AUTH_USERNAME
        sync: false
      - key: AUTH_PASSWORD
        sync: false
    region: oregon
    dockerContext: .
    dockerfilePath: ./Dockerfile
    autoDeployTrigger: commit
